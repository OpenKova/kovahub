{
  "name": "@kovahub/backend",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/jwt": "^9.1.0",
    "@fastify/multipart": "^10.0.0",
    "fastify": "^5.3.2",
    "fflate": "^0.8.2",
    "pg": "^8.20.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
